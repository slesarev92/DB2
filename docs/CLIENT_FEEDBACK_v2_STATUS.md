---
title: Аудит "Что уже сделано в коде vs MEMO v2.1"
date: 2026-05-12
memo: docs/CLIENT_FEEDBACK_v2.md
based_on: tag v2.4.0 (2026-04-15) + main HEAD
---

# Статус MEMO v2.1 в кодовой базе

Сводный аудит 7 блоков MEMO от 2026-04-23 против актуального
состояния кодовой базы (tag `v2.4.0`, main HEAD на 2026-05-12).

Легенда:
- ✅ Сделано
- 🟡 Частично
- ❌ Не сделано
- ⚠️ Регрессия / сломано
- ❓ Требует уточнения у заказчика

---

## Блок 1 — ОСНОВА / СОДЕРЖАНИЕ

### 1.1 Баги

| Пункт | Статус | Доказательство / Заметки |
|---|---|---|
| RoadMap: Гантт не перестраивается при смене статуса | ⚠️ | `frontend/components/projects/gantt-chart.tsx` рендерит цвет через `STATUS_COLORS[entry.status]`. Компонент перерисуется только если родитель пересоздаст массив `tasks`. Если статус хранится в JSONB `Project.roadmap_tasks` и мутируется in-place — React не увидит. Регрессионный код не нашёл, но баг известен из MEMO. |
| Финплан: интермиттирующая ошибка при сохранении | ⚠️ | `backend/app/services/financial_plan_service.py:replace_plan` делает `DELETE` + `INSERT` + `flush`. Если SCALAR opex и `opex_items` рассинхронизированы, или Pydantic не парсит пустые строки — упадёт. UI sanitize (frontend `financial-plan-editor.tsx:174`) пытается чинить, но это compensating logic — корневая причина не найдена. Серверного логирования payload нет. |
| CAPEX = 0 → краш приложения | ⚠️ | `financial-plan-editor.tsx:201` `Number(i.capex \|\| 0)` — на фронте обработано. На бэке `replace_plan` принимает 0 без проверок (Numeric). Где именно баг — не зафиксировано. Регрессия из MEMO остаётся открытой. |

### 1.2 UX / интерфейс

| Пункт | Статус | Доказательство / Заметки |
|---|---|---|
| Перевести все лейблы на русский | ✅ | Закрыто C #31 (2026-05-16). Спот-чек прошёл — найдены и зафиксированы 4 остатка: `Bill of Materials` → `Состав (BOM)` (bom-panel.tsx), `Gantt chart` → `Диаграмма Ганта` (gantt-chart.tsx), `Growth opportunity` → `Возможность роста` (content-tab.tsx), `aria-label="Tier override"` → `"Переопределение тира"` (explain-kpi-inline.tsx). AKB Universe/Target/Coverage оставлены как industry-термины Nielsen. |
| Добавить единицы измерения (шт, л, %, ₽, с/без НДС) | 🟡 | В `financial-plan-editor.tsx` колонки подписаны "CAPEX, ₽"; в `bom-panel`, `pricing-tab` встречаются единицы. Систематической проверки нет — часть полей без "₽/шт/%". |
| Collapse/expand разделов | ✅ | Закрыто C #22 (2026-05-16). Section-level collapse добавлен на 5 табах группы «Анализ» (Results, Sensitivity, Pricing, Value-chain, P&L) через wrapper `<CollapsibleSection>` поверх `@base-ui/react/collapsible`. Persistence — localStorage по ключу `db2:analysis-collapse:v1` (projectId × tabKey × sectionId). Bulk toggle «Свернуть/Развернуть всё» в табах с >1 секциями (Results 10, Sensitivity 4, Pricing 3). Локальные row-level раскрытия не тронуты. См. spec `docs/superpowers/specs/2026-05-16-c22-analysis-collapsible-design.md`. |
| Статус проекта = dropdown | ❌ | `Project` модель имеет `gate_stage` (G0..G5), но не общий "статус". В UI `gate-timeline.tsx` рисует фиксированную шкалу — выпадающего списка для смены статуса нет. Цветовые метки на ячейках Гантта по статусу — есть (`gantt-chart.tsx:STATUS_COLORS`). |
| Ручная раскраска ячеек Гантта (жёлтый/зелёный/красный) | ❌ | `gantt-chart.tsx` не имеет UI для ручной заливки. |

### 1.3 Блокеры — вопросы к заказчику (НЕ должны быть "сделано")

| Пункт | Статус | Текущее состояние в коде |
|---|---|---|
| Что такое "Тип производства" в расчётах? | ❓ | `ProjectSKU.production_mode` ∈ {"own","copacking"} — миграция `6c10a01c69c0`. В s03_cogs (`backend/app/engine/steps/s03_cogs.py:41-58`) логика взаимоисключения реализована: `copacking → production=0`, иначе `copacking=0`. Поле работает, но описание для пользователя в UI отсутствует — нужно подтверждение семантики. |
| "Источник" в Согласующих | ❓ | `Project.approvers` — JSONB list. Структура произвольная, поле "источник" не описано в коде. Никак не используется в расчётах. |
| MOQ — расчётный или справочный? | ❓ | MOQ упомянут ТОЛЬКО в `supplier_quotes` JSONB (Phase 8.10) как описание формата котировок. Расчётно нигде не задействован. По умолчанию — справочный. |
| OBPPC — перенести из Дистрибуции в Содержание | ✅ | Закрыто C #13 Q4 (2026-05-16). `obppc` перемещён из `distribution.tabs` в `basics.tabs` после `content` в `frontend/lib/project-nav-context.tsx`. БД/API/сервис без изменений, FK сохранён. См. spec `docs/superpowers/specs/2026-05-16-c13-q4-obppc-in-contents-design.md`. |

### 1.4 Будущие версии

| Пункт | Статус | Доказательство |
|---|---|---|
| Бенчмарки через Excel/AI/Nielsen | 🟡 | `nielsen_benchmarks: JSONB` уже есть на `Project` (Phase 8.9). C #30 (2026-05-16): добавлена Pydantic-модель `NielsenBenchmarkItem` с опциональным `source_type: Literal["manual", "excel", "ai", "nielsen"]` + `extra="allow"` для гибкости UI. Готовит почву для #16 (импорт Nielsen). |

---

## Блок 2 — ОСНОВА / ФИНАНСОВЫЙ ПЛАН

### 2.1 Детализация CAPEX и OPEX

| Пункт | Статус | Доказательство / Заметки |
|---|---|---|
| Y1-Y3 помесячно, Y4-Y10 годами | ❌ | `ProjectFinancialPlan` хранит запись `(project_id, period_id, capex, opex)`. `financial_plan_service._get_first_period_by_year` маппит **только первый период каждого года** (Y1=M1, Y2=M13, Y3=M25, Y4..Y10=сами годы). UI редактирует 10 строк Y1..Y10. Помесячная гранулярность для Y1-Y3 отсутствует. |
| CAPEX: статьи затрат (Молды, Линия розлива, ...) с add/remove | ❌ | В таблице `project_financial_plans` поле `capex` — одна Numeric. Статей нет. |
| OPEX: статьи (Digital, E-com, SMM, ...) | 🟡 | Таблица `opex_items` (миграция `0bd00f1c9b93` + `338f0a242518`) — есть `category` + `name` + `amount`. UI `financial-plan-editor.tsx` позволяет добавлять/удалять статьи через "Разбить". Категории — `OPEX_CATEGORIES` enum во frontend. Помесячно по Y1-Y3 — НЕТ. |
| Принцип разнесения CAPEX/OPEX в UI | ❌ | Текстового пояснения / help-кнопки рядом с CAPEX и OPEX вне Help-tooltips нет. |

---

## Блок 3 — ПРОДУКТ / SKU и BOM

### 3.1 Баги

| Пункт | Статус | Заметки |
|---|---|---|
| Ошибки ввода в Производство/КАиУР/Маркетинг | ⚠️ | Поля есть в `ProjectSKU` (production_cost_rate, ca_m_rate, marketing_rate, все Numeric(8,6)). Конкретный баг в коде не локализован. |
| Загрузка изображения SKU + AI генерация | 🟡 | `sku-image-upload.tsx` загружает через `/media`. AI-генерация мокапов — `mockup-gallery.tsx` (Phase 7.8, миграция `e3be6ed2aeaa`). Баг "не загружается" — не воспроизведён в коде. |
| Баг верстки при загрузке лого | ⚠️ | Не локализован. |
| Удаление проекта не удаляет SKU из каталога | ✅ | `ProjectSKU.sku_id → SKUs.id ON DELETE RESTRICT` (entities.py:421). При удалении проекта SKU остаются в каталоге — это **намеренно по дизайну** (SKU = справочник). MEMO просит сделать это поведением UX, не FK. Модального предупреждения "удалить SKU/оставить" — НЕТ. |

### 3.2 Логика производства (КРИТИЧНО)

| Пункт | Статус | Доказательство |
|---|---|---|
| Copacking → Production cost = 0; иначе Copacking = 0 | ✅ | `backend/app/engine/steps/s03_cogs.py:41-62` — реализовано. Миграция `6c10a01c69c0` добавила `production_mode` + `copacking_rate` на ProjectSKU. |
| UI dimming полей Производство при копакинге | 🟡 | `bom-panel.tsx:294-391` — production_mode radio с auto-save. Dimming/disable конкретных полей при копакинге — нужно проверить визуально, в коде явного `disabled={productionMode === 'copacking'}` для всех полей Производство не найдено. |
| Косты копакера vs своего производства — не сосуществуют | ✅ | s03_cogs гарантирует через `if is_copacking`. |
| CA&M и Маркетинг (персонал) → перенести в Каналы | ❌ | Архитектурный вопрос, решение не зафиксировано. Сейчас `ca_m_rate` / `marketing_rate` — на ProjectSKU (per-SKU), не per-channel. |

### 3.3 Поля и названия

| Пункт | Статус | Доказательство |
|---|---|---|
| Тип упаковки — справочник (ПЭТ/Стекло/Банка/Сашет/Стик/Пауч) | ✅ | Закрыто C #19 (2026-05-16). SKU.format → Literal['ПЭТ', 'Стекло', 'Банка', 'Сашет', 'Стик', 'Пауч'] на уровне Pydantic, CHECK constraint `ck_skus_format` на уровне БД. Migration с fuzzy backfill existing значений. Frontend Select в add-sku-dialog. Spec: `docs/superpowers/specs/2026-05-16-c19-pack-format-enum-design.md`. |
| Переименовать "Формат" → "Вложение в кейс" | ❌ | `SKU.format: String(100)` — в коде остался как `format`. |
| Структура Бренд/Суббренд/Вкус/Тип упаковки/Объём отдельными полями | 🟡 | Есть: `brand`, `name`, `volume_l`, `package_type`, `segment`. Поля "суббренд", "вкус" — отдельных нет, всё внутри `name`. Фильтрация по этим элементам ограничена. |
| НДС 22% дефолт + 10% на сахар | 🟡 | Миграция `7f562d5f84d2` добавила `BOMItem.vat_rate` с **server_default 0.20**, не 0.22. Per-ingredient VAT хранится, но **не используется в engine** (Grep по `vat_rate` в `app/engine` показывает только `inp.vat_rate` = project-wide). Расчёт всё ещё через `Project.vat_rate` (default 0.20). |
| Минимум 2 знака после запятой в ценах | ❓ | `Numeric(15,4)` для цен — точность достаточна. Форматирование `formatMoney`/`formatNumber` — не проверял, возможно есть toFixed(2). |

### 3.4 Справочная панель BOM

| Пункт | Статус | Заметки |
|---|---|---|
| Сводка Сырьё/Материалы/Прочее/Итого в правом углу BOM | ❌ | `bom-panel.tsx` имеет items но отдельной summary-карты с разбивкой по категориям не найдено. |
| Документировать логику красной подсветки | ❓ | Подсветка существует (видимо в bom-panel), пороги не задокументированы. |

---

## Блок 4 — ДИСТРИБУЦИЯ / КАНАЛЫ

### 4.1 Каталог каналов

| Пункт | Статус | Доказательство |
|---|---|---|
| Редактируемый каталог каналов | 🟡 | `Channel` таблица + CRUD endpoints (`api/channels.py`). Frontend `channels-panel.tsx` показывает список. Редактирование — есть. |
| Кастомный канал | ✅ | Через CRUD POST `/api/channels`. |
| Чекбоксы с группировкой HM/SM/MM/TT/E-COM | ❌ | `channels-panel.tsx` не имеет группировки по группам. `Channel` модель не имеет поля `group` / `category`. |
| Источник данных (Nielsen/ЦРПТ/2GIS/Infoline/кастомный) | ❌ | Поля `source_type` на `Channel` нет. |
| Баг верстки в названии канала | ⚠️ | Не локализован. |

### 4.2 Офтейки и рамп-ап

| Пункт | Статус | Доказательство |
|---|---|---|
| Рамп-ап офтейков (Y1-Y3 по месяцам) | ✅ | `predict_service.py:_ramp_values` (D-10/D-11). Параметр `nd_ramp_months` на `ProjectSKUChannel`. Линейная интерполяция от 20% target до target. Произвольный профиль по месяцам — нет, только линейный. |
| Каталог сезонности | ✅ | `RefSeasonality` (entities.py:82) + JSONB `month_coefficients`. `ProjectSKUChannel.seasonality_profile_id` (миграция `1c05696e13e6` initial). Применяется в `s01_volume` для monthly периодов. |
| AI-расчёт сезонности из Nielsen/ЦРПТ | ❌ | Не реализовано, помечено как будущая версия. |

### 4.3 АКБ

| Пункт | Статус | Доказательство |
|---|---|---|
| АКБ = план (не справочник) | 🟡 | Таблица `akb_entries` (миграция `196823092dd6`) — есть, хранит план дистрибуции per (project × channel) с `target_outlets`, `coverage_pct`. Frontend `akb-tab.tsx`. |
| Автоматический расчёт АКБ = % дистрибуции × ОКБ | ❌ | Авто-связь "АКБ строится из ProjectSKUChannel.nd_target" — нет. Сейчас АКБ заполняется вручную. |
| Раздельные ОКБ (общая) и АКБ (план) | 🟡 | `AKBEntry.universe_outlets` (ОКБ) + `target_outlets` (АКБ). Семантика смешанная. |

### 4.4 Прочее

| Пункт | Статус | Доказательство |
|---|---|---|
| Устранить дублирование ввода SKU между "SKU и BOM" и "Каналы" | ❌ | `channels-tab.tsx` всё ещё имеет свою логику привязки. FK с RESTRICT обеспечивает целостность, но UI не объединён. |

---

## Блок 5 — МОДЕЛИРОВАНИЕ

### 5.1 Fine Tuning

| Пункт | Статус | Доказательство |
|---|---|---|
| Переименовать "Периоды" → "Fine Tuning" | ✅ | `project-nav-context.tsx:52` `periods: "Fine tuning"`. Внутреннее имя tab — `periods`, видимое — "Fine tuning". |
| Тюнинг тарифа копакинга по периодам | 🟡 | `s03_cogs` использует `copacking_per_unit` (скаляр), не per-period массив. Pипe-line использует `production_cost_rate` per-period (D-19), но `copacking_rate` — скаляр. Per-period копакинг — НЕТ. |
| Тюнинг логистики (руб/л) | 🟡 | `s05_contribution` — логистика per-period присутствует (через PeriodValue или константу). Frontend UI для тюнинга — `periods-grid` редактирует ND/Offtake/Shelf, не logistics_per_l. |
| Тюнинг CA&M, Маркетинг, Цена отгрузки | 🟡 | Цена/Shelf — редактируется в periods-grid. CA&M/Marketing — нет per-period. |
| Себестоимость 3 уровня по периоду | ❌ | См. 5.2. |
| Переход копакинг → своё с указанного периода | ❌ | `production_mode` — boolean скаляр на ProjectSKU, не per-period. Невозможно сменить с M12 без полной смены SKU. |

### 5.2 Себестоимость: 3 уровня

| Пункт | Статус | Заметки |
|---|---|---|
| BOM cost_level (max/normal/optimal) | ❌ | Grep по `cost_level` пустой. На `BOMItem` нет поля для уровня. Архитектура не выбрана. |

### 5.3 Сценарии

| Пункт | Статус | Доказательство |
|---|---|---|
| Перенести из Моделирование → в Анализ/Результаты | ✅ | Закрыто C #24 (2026-05-16). `project-nav-context.tsx`: `scenarios` перемещён из `modeling.tabs` в `analysis.tabs` (первой позицией, до `results`). |
| Название сценария (текстовое поле) | ✅ | Закрыто C #24 (2026-05-16). Migration `b9986ce73ab2` добавила `scenarios.name VARCHAR(200) NULL`. Pydantic `ScenarioRead.name` + `ScenarioUpdate.name`. UI input в `scenarios-tab.tsx` (placeholder = type-label fallback когда name=NULL). |
| Интермиттирующая ошибка | ⚠️ | Не локализована. Defer до репродукции. |
| Документировать как сценарий влияет на расчёт | ✅ | Scenario = дельты (`delta_nd`, `delta_offtake`, `delta_opex`, `delta_shelf_price`, `delta_bom_cost`, `delta_logistics`) поверх base — миграция `3e5dcbc50271`. Применяются в `calculation_service:412-418`. Базовый расчёт не перезаписывается. C #24 (2026-05-16): добавлена пояснительная подсказка в UI `scenarios-tab` (CardDescription секции «Дельты сценариев»). |

---

## Блок 6 — АНАЛИЗ / РЕЗУЛЬТАТЫ

### 6.1 P&L

| Пункт | Статус | Доказательство |
|---|---|---|
| Структура P&L = лист DATA Excel | ✅ | `schemas/pnl.py:PnlPeriod` имеет: NR → COGS_material/production/copacking → GP → logistics → contribution → CA&M → marketing → EBITDA → WC/Tax/OCF/FCF. Соответствует Excel DATA. Endpoint `GET /api/projects/{id}/pnl` (projects.py:293). |
| Логика инфляции (апрель/октябрь +5%) | ✅ | `RefInflation.month_coefficients` JSONB + `predict_service.inflate_series` применяет к shelf и BOM (s03_cogs docstring). Шаг — настраиваемые `month_coefficients` (массив 12) + `yearly_growth` (массив 7). Применяется к: shelf_price, bom_unit_cost. |
| Применяется ли к ценам отгрузки тоже? | ✅ | Да — `predict_service:218` инфлирует `shelf_series`. |
| Фильтры P&L (SKU/канал/бренд/объём/вкус) + pivot | ❌ | `PnlResponse` — агрегированный per-period для всех SKU. Разреза по SKU/каналу/бренду — нет в endpoint. Frontend `pnl-tab.tsx` показывает агрегат. |
| Экспорт pivot P&L в Excel | ❌ | Excel exporter имеет один лист "PnL по периодам", не pivot. |

### 6.2 Чувствительность NPV

| Пункт | Статус | Доказательство |
|---|---|---|
| 3 горизонта Y1-Y3/Y1-Y5/Y1-Y10 + переключатель | ✅ | `sensitivity_service.py:86` `VALID_SCOPES = ("y1y3","y1y5","y1y10")`. Endpoint принимает `scope` param. Frontend `sensitivity-tab.tsx` имеет переключатель. |
| Раскраска таблицы зелёный/красный с настраиваемыми порогами | 🟡 | `tornado-chart.tsx` существует. Текущая палитра — есть, настраиваемых порогов в UI — нет. |
| 4 параметра: Цена/Себестоимость ±10%, WACC ±1%, CAPEX ±10% | ✅ | См. sensitivity_service — параметры захардкожены. |
| D-12 Y1-Y5 = 6 лет (Excel-тайпо) | ⚠️ | **Регрессия / расхождение документации**: `s11_kpi.py:50` `SCOPE_BOUNDS["y1y5"] = (5,5)` с комментарием "исправлено с 6 на 5 одобрено 2026-04-13", НО `docs/ROADMAP.md` "Технические долги" и MEMO 7.1.3 говорят "Excel-тайпо сохранён намеренно". Источники истины расходятся — требует уточнения. |

### 6.3 Стакан / Unit-экономика

| Пункт | Статус | Доказательство |
|---|---|---|
| Переименовать "Стакан" → "Unit-экономика" | ✅ | `SECTION_LABELS["value-chain"] = "Unit-экономика"`. |
| Waterfall-диаграмма | ❌ | `value-chain-tab.tsx` — табличная структура. Файла waterfall-компонента нет (grep по `waterfall` дал 4 файла, но это просто упоминания/комментарии — не реализованный chart). |
| Настройка цветов GP/CM/EBITDA | ❌ | Не реализовано. |
| Разрезы по каналам HM/SM/MM/TT/E-COM | 🟡 | `value_chain_service.build_value_chain` группирует per-SKU per-channel. Per-канал есть, группировки по группам каналов (HM/SM/MM) — нет. |
| Фильтры SKU/канал/период | 🟡 | Базовые фильтры в `value-chain-tab.tsx` есть. По периоду (Y1-Y3 vs Y1-Y10) — нужно проверить визуально. |

### 6.4 Экспорт (КРИТИЧНО)

| Пункт | Статус | Доказательство |
|---|---|---|
| Экспорт не работает (MEMO: сломан) | ⚠️ | Endpoints `/export/xlsx`, `/export/pptx`, `/export/pdf` существуют (projects.py:401-564). На уровне роутов работает (тесты в `tests/export/` есть). Конкретный продакшен-баг "не работает" не воспроизведён в коде. Может быть UI button issue — нужна проверка через браузер. |
| Раздельные PDF и Excel endpoints | ✅ | Endpoints разделены (XLSX, PPTX, PDF). |
| PDF: KPI + P&L + Стакан + Чувствительность + План продаж + АКБ | 🟡 | `pdf_exporter.py` — 12 секций (по docstring projects.py:535). Не все 6 пунктов MEMO явно прописаны. План продаж и АКБ — частично. |
| Чекбоксы "какие разделы включать в PDF" | ❌ | UI выбора секций нет. |

### 6.5 Прочее

| Пункт | Статус | Заметки |
|---|---|---|
| кг/л через слеш (не дублировать) | ❌ | `SKU.volume_l` хранит литры. Унифицированного отображения "кг/л" в UI нет. |
| Агрегация проектов | ❌ | Backlog (`BL-10`). Все данные имеют `project_id`, агрегация архитектурно возможна, но не реализована. |

---

## Блок 7 — ДВИЖОК / ENGINE

### 7.1 Критичные правила

| Пункт | Статус | Доказательство |
|---|---|---|
| Copacking=0 если own; Production=0 если copacking | ✅ | `s03_cogs.py:41-62` — реализовано через `is_copacking`. Тесты `test_steps_1_5.py` покрывают. |
| Go/No-Go CM threshold настраиваемый | ✅ | Миграция `32c6c1bd0abf` добавила `Project.cm_threshold` с server_default 0.25. UI для редактирования — нужно проверить (project overview tab). |
| Все пороги Go/No-Go в одном месте (NPV>0, IRR>WACC) | 🟡 | Только `cm_threshold` хранится в БД. NPV>0 и IRR>WACC — захардкожены в `s12_gonogo.py`. |
| D-12 Y1-Y5 = 6 лет (документировать или исправить) | ⚠️ | См. 6.2 — расхождение комментария в коде vs ROADMAP. Tooltip на UI с пояснением "Расчёт ведётся от даты старта SKU" — нет. |

### 7.2 Будущие версии (не блокирует)

| Пункт | Статус | Заметки |
|---|---|---|
| Каннибализация / price elasticity / промо / перенос налогов | ❌ | По MEMO — игнорируем. |
| Scenario deltas только по ND/offtake | 🟡 | Сейчас Scenario имеет также `delta_shelf_price`, `delta_bom_cost`, `delta_logistics` (миграция `3e5dcbc50271`). По MEMO — оставить только ND/offtake, остальное считается в чувствительности. Не вычищено. |
| Tax loss carryforward (ст.283 НК РФ) | ✅ | Миграция `d894b52f645b`, флаг `Project.tax_loss_carryforward` (default False). Phase 4.1 закрыта. |
| Валидация вводных (отрицательная цена, нулевой universe) | ❌ | Отсутствует. Сейчас расчёт продолжается на любых входах. |

---

## Сводка по приоритетам

### 🔴 Критично (из MEMO + найденные регрессии)

1. **D-12 рассинхрон** (Блок 6.2 / 7.1.3, ⚠️) — `s11_kpi.py` говорит "исправлено 6→5", ROADMAP "оставлен намеренно". До решения этого вопроса нельзя менять KPI logic. **Требует ответа заказчика.**
2. **4 блокера 1.3** (❓) — `production_mode`, "Источник", MOQ, OBPPC перенос. Все 4 — ответы заказчика, не разработка.
3. **Финплан / CAPEX баги 1.1** (⚠️) — нет серверного логирования payload, без него корневую причину не найти.
4. **Экспорт не работает** (6.4, ⚠️) — на уровне роутов всё есть. Нужна верификация в браузере, может быть UI-баг.
5. **CA&M / Маркетинг в Каналы** (3.2) — архитектурное решение блокирует P&L переработку.

### 🟠 Высокий

6. **CAPEX/OPEX помесячно Y1-Y3 + статьи CAPEX** (Блок 2.1, ❌) — большой объём работы. Сейчас только годовая гранулярность + OPEX-статьи.
7. **Fine Tuning по периодам** (5.1, 🟡) — тариф копакинга, логистика, CA&M, marketing per-period не редактируются. UI ограничен ND/Offtake/Shelf.
8. **P&L фильтры + pivot Excel** (6.1, ❌) — endpoint возвращает агрегат, разрезов нет.
9. **НДС 22%** (3.3, 🟡) — server_default `BOMItem.vat_rate` = 0.20, не 0.22. `Project.vat_rate` default 0.20. Per-ingredient VAT не используется в engine. **С 01.01.2026 РФ ставка 22%** — это уже текущая.
10. **3 уровня себестоимости** (5.2, ❌) — нет ни поля, ни UI.
11. **Группы каналов + источник данных** (4.1, ❌) — `Channel` без `group` и `source_type`.
12. **Структурные SKU поля для фильтрации** (3.3, 🟡) — суббренд/вкус слиты в `name`.

### 🟡 Средний

13. **Collapse/expand разделов** (1.2, ✅ — закрыто C #22 2026-05-16).
14. **Статус проекта dropdown + ручная раскраска Гантта** (1.2, ❌).
15. **Waterfall-диаграмма стакана** (6.3, ❌).
16. **АКБ автоматический расчёт из nd × ОКБ** (4.3, ❌).
17. **Раскраска чувствительности с настраиваемыми порогами** (6.2, 🟡).
18. **Тип упаковки → справочник enum** (3.3, ✅ — закрыто C #19 2026-05-16).
19. **Перенос Сценариев в Анализ + название** (5.3, ✅ — закрыто C #24 2026-05-16; интермиттирующая ошибка ⚠️ defer'нута до репродукции).
20. **Дублирование ввода SKU между табами** (4.4, ❌).
21. **Единицы измерения systematically** (1.2, 🟡).

### 🟢 Низкий

22. **BOM сводка справа** (3.4, ❌).
23. **PDF чекбоксы выбора секций** (6.4, ❌).
24. **кг/л через слеш** (6.5, ❌).
25. **Агрегация проектов** (6.5, ❌, = BL-10).
26. **Документировать красную подсветку BOM** (3.4, ❓).
27. **Валидация вводных** (7.2, ❌).
28. **Источник для бенчмарков заранее** (1.4, 🟡).
29. **Финальная русификация спот-чек** (1.2, ✅ — закрыто C #31 2026-05-16).
30. **Scenario deltas очистка от price/COGS/log** (7.2, 🟡) — если решение "только ND/offtake" финальное.
